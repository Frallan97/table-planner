{{/*
  The database secret must be created manually before deploying.
  See NOTES.txt for instructions.
  
  This template is intentionally empty - it serves as documentation
  that the secret is expected to exist.
*/}}
